services:
  - type: web
    name: laravel-app
    env: php
    plan: free
    buildCommand: "composer install --no-dev && php artisan key:generate && php artisan config:cache && php artisan route:cache"
    startCommand: "php artisan serve --host 0.0.0.0 --port $PORT"
